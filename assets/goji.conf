{
	"marathon-host": "${MARATHON_HOST}",
	"marathon-port": ${MARATHON_PORT},
	"services": [
		{ "app-id": "${VARNISH_APP_ID}", "name": "varnish", "protocol": "${VARNISH_PORT_PROTOCOL}", "port": ${VARNISH_PORT} }
	],
	"template": "/conf/varnish-agent.tmpl",
	"target": "/var/www/html/config.json"
}